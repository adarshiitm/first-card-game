http:
 port: 8080
 adminPort: 8081
 connectorType: nonblocking
 #minThreads: 20
 #maxThreads: 1024
 #acceptorThreads: 5
 requestLog:
     timeZone: IST
     file:
       enabled: true
       timeZone: IST
       currentLogFilename: /var/log/adarsh/card/card-game/requests.log
       archive: true
       archivedLogFilenamePattern: /var/log/adarsh/card/card-game/requests.log.%d{yyyy-MM-dd}

logging:
  level: INFO
  file:
   enabled: true
   timeZone: IST
   logFormat: "%date %level [%thread] %logger{0} [%X{id}] %msg%n"
   currentLogFilename: /var/log/adarsh/card/card-game/card-game.log
   archive: true
   archivedLogFilenamePattern: /var/log/adarsh/card/card-game/card-game.log.%d{yyyy-MM-dd}
   archivedFileCount: 20

database:
  # the name of your JDBC driver
  driverClass: com.mysql.jdbc.Driver
  # the username
  user: root
  # the password
  #password:
  # the JDBC URL
  url: jdbc:mysql://localhost/card_game
  # any properties specific to your JDBC driver:
  properties:
   charSet: UTF-8
   hibernate.show_sql: false
   hibernate.hbm2ddl.auto: validate
  # the maximum amount of time to wait on an empty pool before throwing an exception
  maxWaitForConnection: 1s
  # the SQL query to run when validating a connection's liveness
  validationQuery: "/* MyApplication Health Check */ SELECT 1"
  # the minimum number of connections to keep open
  minSize: 8

  # the maximum number of connections to keep open
  maxSize: 32
  # whether or not idle connections should be validated
  checkConnectionWhileIdle: false
  closeConnectionIfIdleFor: 1 minute

